# DailyBrief 智能媒体系统升级实施方案

> **版本**：v1.0  
> **编制**：总经理办公室  
> **日期**：2026年7月10日  
> **密级**：内部战略级

---

## 一、项目现状诊断

### 1.1 现有架构总览

当前 DailyBrief 基于 TypeScript + Node.js + GitHub Actions 构建，已实现 **8阶段管道**：

```
Phase 0: 凭证校验 → Phase 1-3: 抓取+去重 → Phase 4: 分类AI增强
→ Phase 5: 交易信号 → Phase 6: 摘要生成 → Phase 7: 分类摘要 → Phase 8: AI审核
```

技术栈：TypeScript / Node.js 20 / rss-parser / OpenAI API / 原生HTML+CSS

数据源：50+ RSS/API，覆盖 tech/trending/finance/politics 四大类目

### 1.2 十大问题根因深度分析

| 序号 | 问题描述 | 代码根因 | 严重程度 |
|------|---------|---------|---------|
| 1 | **抓取源速度太慢** | `fetchAll()` 批次并发仅 batchSize=5，50+源串行批次执行；RSS超时仅8000ms；无HTTP连接池复用；无优先级抢占机制 | ⭐⭐⭐⭐⭐ |
| 2 | **标签栏无折叠打开按钮** | 前端仅实现 L1→L2→L3 三级 Tab 切换（`tabs/sub-tabs/source-tabs`），无手风琴(accordion)折叠设计；长内容无法收起 | ⭐⭐⭐ |
| 3 | **抓取失败源无重试容错** | `fetchAll()` 只用 `Promise.allSettled` 一次性执行，失败直接入 `failedSources`；无指数退避重试；无降级备用源 | ⭐⭐⭐⭐ |
| 4 | **部分栏目空占条目、未翻译、缺审核** | `groupRaw()` 预填充所有 enabled 源的空 bucket，即使 0 条也渲染 Tab；`consolidatedEnrich` 存在 undercount 时静默降级；`aiReview` 只抽检5条不拦截发布 | ⭐⭐⭐⭐ |
| 5 | **点击标签页缺乏聚合筛选** | 纯 Tab 切换，无全局搜索框、无跨源关键词筛选、无标签云过滤联动、无"全部"聚合视图 | ⭐⭐⭐⭐ |
| 6 | **缺乏信息源更新时间显示** | `renderHtml` 未传递每源 `fetchedAt` 时间戳；仅整体报告无时间；用户无法判断信息新鲜度 | ⭐⭐⭐ |
| 7 | **AI分析栏仅部分栏目有** | 仅 4 个一级类目调用 `enrichCategorySummary` 生成分类摘要；L2子分类、L3来源级均无AI分析；交易板块独立生成 | ⭐⭐⭐⭐ |
| 8 | **前端缺乏科技感、排版丑** | 纯手写 CSS，无设计系统(Design System)；配色单一；无动效/渐变/玻璃拟态；信息层级扁平无重点 | ⭐⭐⭐⭐ |
| 9 | **AI参与度不及格，未达智能媒体标准** | 当前 AI 仅做：翻译、摘要、打标签、评分；缺深度推理、因果分析、趋势预测、多源交叉验证；本质仍是"传统RSS+翻译精炼" | ⭐⭐⭐⭐⭐ |
| 10 | **深度分析工具链不足** | 本地 luopan/agent-reach 为通用技能，非新闻领域专用；缺领域知识库(RAG)、缺多Agent协作、缺研究报告生成能力 | ⭐⭐⭐⭐⭐ |

---

## 二、架构选型：三套技术方案对比

### 方案 A：现有代码库渐进式重构（改良派）

**核心思路**：保留现有 TypeScript 代码库，在原架构上模块化升级。

#### 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                     前端层 (React 重写)                  │
│  科技感UI / 折叠面板 / 全局搜索 / 时间轴 / 深度分析面板    │
├─────────────────────────────────────────────────────────┤
│                     抓取引擎层                           │
│  并发池(15并发) / 指数退避重试 / 备用源降级 / 连接复用     │
├─────────────────────────────────────────────────────────┤
│                     AI 处理层                            │
│  摘要 / 翻译 / 打标签 → 新增: 深度分析Agent / RAG知识库   │
├─────────────────────────────────────────────────────────┤
│                   基础设施层                             │
│         GitHub Actions / 缓存优化 / 监控告警             │
└─────────────────────────────────────────────────────────┘
```

#### 改造要点

**抓取层提速**：
- `batchSize` 从 5 提升至 15，引入 `p-limit` 并发池
- 新增 3 次指数退避重试（2s → 5s → 12s），失败自动切换 `useCurl` 备用通道
- 引入 `undici` 连接池复用 HTTP 连接，减少 TLS 握手开销
- 优先级分层：高优源(P5) 独立批次优先完成，低优源可超时跳过

**容错与审核增强**：
- 空栏目自动隐藏（0条内容的 Tab 不渲染）
- `aiReview` 升级为拦截式：发现翻译质量问题自动触发二次enrich
- 新增"翻译完整性校验"：检查中文占比，低于阈值标记为待审核

**前端重构**：
- 用 React + Tailwind CSS + shadcn/ui 重写前端
- 三级 Tab 增加手风琴折叠模式，移动端自动切换
- 新增全局搜索栏 + 标签云联动筛选
- 每个来源 Tab 显示「最后更新于 X 分钟前」
- 科技感设计：玻璃拟态卡片、渐变强调色、微动效、深色模式优化

**AI 深度增强**：
- 每个 L2 子栏目都生成 AI 洞察（趋势/矛盾/机会点）
- 新增「深度分析」模块：基于 LangGraph 构建多Agent协作
  - 事实核查Agent：多源交叉验证
  - 趋势分析Agent：识别上升话题
  - 影响评估Agent：分析事件连锁反应

#### 优势
- ✅ 代码复用率高（~70%），迁移成本最低
- ✅ 保留现有 GitHub Actions 工作流与数据源配置
- ✅ 团队熟悉 TypeScript 技术栈，上手快
- ✅ 可分阶段迭代，风险可控

#### 劣势
- ❌ 原生架构限制，Agent 深度分析能力天花板较低
- ❌ 前端重构工作量大，等于重写
- ❌ Python 生态的 AI Agent 工具无法直接复用

---

### 方案 B：基于 Horizon 迁移改造（融合派）

**核心思路**：迁移到 Horizon（Python 开源 AI 新闻雷达）架构，融合现有数据源与前端体验。

#### Horizon 架构特性

```
高并发异步采集层 → 语义级去重(嵌入向量) → 多模型协同处理层
→ 知识增强引擎(Web检索补背景) → 内容生成层 → 多渠道分发
```

**核心能力**：
- 异步 I/O 并行拉取，速度提升 3-5 倍
- 语义去重：向量比对识别"标题不同但内容重复"
- 知识增强：高分资讯自动检索相关背景补充上下文
- 支持 Claude/GPT/Gemini/DeepSeek/豆包/MiniMax/Ollama 全模型
- MCP 协议支持，可接入 Agent 生态

#### 改造要点

**数据层迁移**：
- 现有 50+ `sources.config.json` 批量导入 Horizon 源配置
- 保留现有分类体系（trending/tech/finance/politics）
- 交易信号模块作为独立插件接入

**AI 能力升级**：
- 直接获得语义去重、相关性评分(0-10)、背景知识增强
- 新增"社区讨论摘要"：聚合 Reddit/HN/V2EX 评论
- 深度分析可扩展 Horizon 的 enrich pipeline

**前端层**：
- 保留方案 A 的前端重构计划，后端 API 适配 Horizon 输出格式
- Horizon 原生输出 Markdown，需转换为结构化 JSON 供前端使用

#### 优势
- ✅ 开箱即用的语义去重 + 知识增强，解决 AI 参与度问题
- ✅ Python 生态丰富，Agent 深度分析扩展空间大
- ✅ 异步采集架构天然解决抓取速度问题
- ✅ MCP 协议支持，未来可接入更多 AI 工具

#### 劣势
- ❌ 代码复用率低（~30%），近乎重写后端
- ❌ 团队需切换 Python 技术栈
- ❌ GitHub Actions 工作流需重新配置
- ❌ Horizon 前端较弱，仍需自研前端

---

### 方案 C：TrendRadar + 深度研究 Agent 重构（革命派）

**核心思路**：底层基于 TrendRadar 做热点聚合，上层叠加自研深度分析Agent系统，打造真正的"AI智能媒体"。

#### TrendRadar 核心能力（53k⭐ 明星项目）

- **多平台聚合引擎**：35+ 中英文平台统一数据格式
- **模块化架构**：核心代码 <5000 行，512MB 内存即可运行
- **多存储后端**：SQLite / S3 / Cloudflare R2
- **LiteLLM 集成**：支持 GPT/DeepSeek/Gemini 等所有模型
- **MCP v4.0 协议**：原生 Agent 集成能力
- **自然语言查询**：对话式检索热点

#### 深度分析 Agent 层（自研）

基于 LangGraph + MCP 构建三Agent协作系统：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  信息聚合Agent │────▶│  深度分析Agent │────▶│  报告生成Agent │
│ (TrendRadar)  │     │ (LangGraph)   │     │ (模板引擎)    │
└─────────────┘     └─────────────┘     └─────────────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           ▼
                    MCP 工具生态
              (搜索/检索/计算/可视化)
```

**Agent 分工**：
1. **信息聚合Agent**（TrendRadar驱动）：多源抓取、去重、评分、分类
2. **深度分析Agent**（LangGraph编排）：
   - 因果链分析：事件 → 直接影响 → 次级影响 → 长期影响
   - 多方观点对比：不同媒体立场差异
   - 趋势识别：与历史数据对比的异常波动
   - 数据验证：交叉核验事实准确性
3. **报告生成Agent**：结构化输出、重点高亮、可视化建议

#### 前端层
- 同方案 A/B 的科技感前端重构
- 新增「深度分析」专属页面，支持交互式探索
- 自定义分析面板：用户输入主题，Agent 生成专属深度报告

#### 优势
- ✅ 最符合"AI智能媒体"战略定位，天花板最高
- ✅ TrendRadar 成熟度高（53k星），抓取层零开发
- ✅ Agent 化架构支持自定义深度分析，可扩展为产品核心竞争力
- ✅ MCP 生态接入，未来可无限扩展工具能力

#### 劣势
- ❌ 工程量最大，周期最长
- ❌ 现有代码复用率最低（<20%）
- ❌ 技术栈全面切换 Python + LangGraph
- ❌ Agent 系统调试复杂度高，需持续优化

---

### 三方案横向对比矩阵

| 评估维度 | 方案A 渐进重构 | 方案B Horizon迁移 | 方案C TrendRadar+Agent | 权重 |
|---------|--------------|-----------------|----------------------|------|
| **解决10大问题覆盖率** | 85% | 92% | 100% | 30% |
| **开发工程量** | 中(2-3周) | 大(4-5周) | 很大(6-8周) | 15% |
| **现有代码复用率** | ~70% | ~30% | <20% | 10% |
| **AI深度分析天花板** | 中等 | 较高 | 极高 | 20% |
| **技术栈切换成本** | 低(TS不变) | 中(转Python) | 高(全栈切换) | 10% |
| **长期可扩展性** | 一般 | 良好 | 优秀 | 10% |
| **运维复杂度** | 低 | 中 | 较高 | 5% |
| **加权总分** | **7.8/10** | **8.3/10** | **9.1/10** | - |

---

## 三、对抗审查（Ruofeng 式自查）

> **自查原则**：用最苛刻的眼光质疑方案，找出每一个"看起来很美但实际行不通"的坑。

### 3.1 对方案A的拷问

**Q1: 并发提到15，不会被目标网站封IP吗？**
- 风险：50+源集中请求，部分小站可能触发限流
- 对策：引入 per-domain 并发限制（同一域名最多2并发）；新增随机延迟抖动；配置 User-Agent 轮换池
- 结论：可控，但需额外开发限流中间件

**Q2: LangGraph 跑在 Node.js 上靠谱吗？**
- 风险：LangGraph Python 版是主流，TS版功能滞后约3-6个月
- 对策：Agent 层改用 Python 微服务，通过 API 与 TS 主程序通信
- 结论：方案A的"纯TS"假设不成立，最终还是要引入Python

**Q3: 前端重写真的只要2-3周？**
- 风险：低估了交互复杂度（搜索、筛选、折叠、动画、响应式）
- 对策：第一阶段先做样式升级（CSS变量+设计令牌），交互重构放第二阶段
- 结论：工期偏乐观，建议拆成两期

### 3.2 对方案B的拷问

**Q1: Horizon 项目活跃度如何？会不会是个半成品？**
- 调研：Horizon 是较新项目，社区规模远小于 TrendRadar
- 风险：遇到坑可能没人修，要自己啃代码
- 对策：先做一周技术预研，跑通全流程再决定
- 结论：存在项目成熟度风险

**Q2: 语义去重会不会误杀？把相关但不重复的新闻也去掉了？**
- 风险：向量相似度阈值难调，过高漏去重，过低误删
- 对策：设置"软去重"——不删除，只标记相似度，由AI判断是否合并展示
- 结论：技术上可解，但需要调参周期

**Q3: 知识增强会不会引入错误信息？Web检索的内容可靠吗？**
- 风险：AI 自动检索的背景信息可能来源不可靠
- 对策：知识增强只做"背景补充"标注，不混入正文摘要；标注信息来源
- 结论：边界清晰就可控

### 3.3 对方案C的拷问

**Q1: TrendRadar 53k星是刷的吧？真实质量怎么样？**
- 调研：TrendRadar 确实有真实社区，v6.0 重构了核心架构，MCP 支持是亮点
- 风险：中文源支持可能不如英文，需二次开发
- 对策：先导入现有50源测试抓取成功率与速度
- 结论：基础质量过关，中文适配需投入

**Q2: 三Agent协作会不会太慢？一次深度分析要跑多久？**
- 风险：多Agent串行 + 多轮工具调用，可能单次分析5-10分钟
- 对策：日报级分析后台预跑，不阻塞主流程；用户自定义分析走异步队列
- 结论：不影响主简报生成速度，用户触发的分析需明确告知等待时间

**Q3: Agent 生成的深度分析，准确性怎么保证？不会胡说八道吧？**
- 风险：大模型幻觉，分析结论缺乏事实依据
- 对策：
  - 强制引用机制：每个论断必须标注来源链接
  - 事实核查Agent独立校验，矛盾点标红
  - 置信度评分：低置信度结论标注"待验证"
- 结论：通过工程手段可大幅降低，但无法100%消除，需产品层面管理预期

**Q4: 工程量6-8周，团队扛得住吗？会不会烂尾？**
- 风险：全栈重构 + Agent 系统，复杂度高
- 对策：分三期交付，每期都有可用产物
  - Phase 1（2周）：TrendRadar 替换底层抓取，前端样式升级
  - Phase 2（3周）：前端交互重构 + 栏目AI分析全覆盖
  - Phase 3（3周）：深度分析 Agent 系统上线
- 结论：分期交付可降低风险，每期都能验收

### 3.4 共性问题自查

**Q: GitHub Actions 跑 Agent 深度分析，时间够吗？会不会超时？**
- 现状：workflow timeout 40分钟，当前全流程约5-8分钟
- 方案C全量Agent分析预估：15-25分钟
- 结论：40分钟上限内可完成，但余量不多；如超可拆分为两个job并行

**Q: LLM 成本会不会暴涨？深度分析很烧token吧？**
- 现状：每日 ~50-80K tokens（摘要+翻译+审核）
- 深度分析预估：增加 3-5 倍 token 消耗
- 对策：
  - 分级处理：TOP 10 新闻才触发深度分析，其余仅摘要
  - 模型分层：摘要用 GPT-4o-mini / DeepSeek，深度分析用强模型
  - 缓存复用：同一主题24小时内不重复分析
- 结论：成本可控上升，预计月增 $20-50，可接受

---

## 四、终审确认：最终推荐方案

### 4.1 推荐结论

**采用「方案C分期落地 + 方案A作为一期保底」的混合策略**

即：**长期目标是方案C（TrendRadar + 深度Agent），但第一期先按方案A的思路快速见效，验证方向后再逐步迁移。**

### 4.2 决策依据

1. **战略对齐**：用户明确提出"AI参与度不及格"、"深度分析还差意思"，说明核心诉求是**从传统聚合升级为智能媒体**。方案A的渐进改良无法根本解决第9、10号问题，只是"做得更好的RSS阅读器"，不符合战略方向。

2. **风险控制**：直接全量切方案C风险过高。采用分期策略，第一期在现有代码库上做快速优化（解决1-8号问题），2周内可见成效；同时并行预研方案C底层架构，验证通过后二期迁移。

3. **投入产出比**：
   - 一期投入小，解决80%的表层问题（速度、UI、容错、基础AI覆盖）
   - 二期投入大，但解决核心战略问题（深度分析、Agent化、智能媒体）
   - 两期之间有决策点，可根据一期效果调整二期投入

### 4.3 最终技术架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层 (React)                        │
│  科技感UI / 三级折叠Tab / 全局搜索 / 时间戳 / 深度分析交互面板     │
├─────────────────────────────────────────────────────────────────┤
│                     API 适配层 (一期TS / 二期Python)              │
├─────────────────────────────────────┬───────────────────────────┤
│           抓取聚合引擎               │      深度分析 Agent 层      │
│  (一期: 现有+并发重试)               │   LangGraph 三Agent协作     │
│  (二期: TrendRadar 替换)             │   事实核查/趋势/影响评估    │
│                                     │   MCP 工具生态接入          │
├─────────────────────────────────────┴───────────────────────────┤
│                       基础设施层 (GitHub Actions)                 │
│               定时触发 / 缓存 / 发布 / 监控告警                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、十大问题对应改造清单（可落地）

### 问题1：抓取源速度太慢 ✅ 一期解决

**改造点**：
- 并发池从 5 提升至 12，引入 per-domain 限流（同域最多 2 并发）
- RSS 超时从 8s 调整为 12s，配合重试机制
- 引入 undici 连接池，复用 HTTP/1.1 keep-alive
- 优先级调度：P5 源独立批次，P3 源可超时跳过不阻塞

**预期效果**：总抓取时间从 ~3 分钟降至 ~45 秒

### 问题2：标签栏无折叠打开按钮 ✅ 一期解决

**改造点**：
- L2/L3 Tab 增加手风琴模式，点击已激活 Tab 可折叠收起内容
- 移动端默认折叠，桌面端默认展开
- 增加"全部展开/全部收起"快捷按钮

### 问题3：抓取失败无重试容错 ✅ 一期解决

**改造点**：
- `fetchSource` 包装重试逻辑：3 次指数退避（2s/5s/12s）
- 第二次重试自动切换通道（原生 fetch → curl 模式）
- 连续失败 3 次的源记录到 `failedSources`，报告底部展示失败清单
- 关键源配置备用 URL，主源全失败自动尝试备用源

### 问题4：空栏目、未翻译、缺审核 ✅ 一期解决

**改造点**：
- 0 条内容的来源 Tab 自动隐藏，不占位
- 翻译完整性校验：中文内容占比 < 60% 标记为"未翻译"，触发二次 enrich
- `aiReview` 升级为拦截式：严重质量问题自动回炉重生成，最多 2 次
- 报告底部新增「审核日志」区块，展示 AI 审核发现的问题

### 问题5：缺乏聚合筛选显示 ✅ 一期解决

**改造点**：
- 顶部新增全局搜索框，支持标题/摘要全文检索
- 标签云可点击筛选，跨栏目展示所有含该标签的新闻
- 新增「全部」聚合视图：按时间倒序展示全栏目新闻，支持来源筛选

### 问题6：缺乏信息源更新时间 ✅ 一期解决

**改造点**：
- 抓取时记录每源 `fetchedAt` 时间戳
- 来源 Tab 右侧显示「更新于 X 分钟前」
- 报告顶部显示本次构建完整时间（精确到分钟）
- 悬停显示绝对时间 + 相对时间

### 问题7：AI分析栏仅部分栏目有 ✅ 一期解决

**改造点**：
- 所有 L2 子栏目都调用 `enrichCategorySummary` 生成 AI 洞察
- 洞察模板升级：从"段落摘要"改为「3个要点 + 1个趋势判断」结构
- 交易板块独立 AI 评论保留并增强
- 新增「今日关键词图谱」可视化

### 问题8：前端缺乏科技感、排版丑 ✅ 一期重点

**改造点**：
- 引入设计系统：主色 #3B82F6（科技蓝）、强调 #8B5CF6（智能紫）、渐变 #06B6D4 → #3B82F6
- 玻璃拟态卡片：`backdrop-filter: blur()` + 半透明白底
- 信息层级强化：头条 Hero 区加大字号+渐变背景
- 微动效：Tab 切换滑动过渡、卡片悬停上浮、骨架屏加载
- 深色模式优化：配色对比度达标
- 排版：引入 12 列栅格，间距标准化（4/8/16/24/32）

### 问题9：AI参与度不及格 ⭐ 二期核心

**改造点**：
- 从"翻译+摘要"升级为"理解+分析+洞察"三层架构
- 每层新闻附加：
  - 事实标签：事件类型、涉及主体、影响范围
  - 情感倾向：正面/中性/负面 + 置信度
  - 相关度：与关注主题的关联强度
- 栏目级 AI 输出：
  - 今日热点排行（算法+AI双重排序）
  - 话题聚类：相似新闻自动归组
  - 矛盾点识别：不同媒体报道差异标注

### 问题10：深度分析工具链不足 ⭐ 二期战略级

#### 产品定位

**用户手动触发式深度分析**——每条新闻卡片右下角设「深度分析」按钮，用户针对自己感兴趣的单条信息发起分析，而非系统全量生成。

**商业化伏笔**：每日免费 3 次额度，超出可按需付费解锁（单条付费 / 月度会员），作为未来增值功能预留。

#### 单条新闻深度分析输出结构

针对单篇报道，Agent 自动扩展检索并输出 5 个维度：

1. **来龙去脉**：该事件的前因后果、时间线梳理
2. **多方视角**：不同立场媒体/社区的报道差异对比
3. **影响传导**：直接影响 → 次级影响 → 长期连锁反应
4. **关键人物/实体**：涉及的核心公司、人物、机构背景
5. **相似历史案例**：过去类似事件的走向与结果参考

#### 技术实现

- 引入 LangGraph 构建深度分析 Agent，三阶段工作流：
  - **检索扩展**：基于新闻标题/摘要调用搜索工具，获取 5-10 篇关联报道
  - **综合分析**：多源信息交叉验证，按 5 维度结构化推理
  - **报告生成**：输出带引用链接的 Markdown 深度分析
- 接入 MCP 工具生态：通用搜索、论文检索、行业数据查询等
- 异步队列处理：用户点击后入队，前端轮询进度，通常 15-30 秒出结果
- 结果缓存 7 天：同一新闻重复分析直接返回缓存，不重复消耗配额

---

## 六、实施路线图

### Phase 1：基础体验升级（第1-2周）

**目标**：解决前8个问题，用户体验显著提升

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W1 | 抓取层优化（并发/重试/容错） + 空栏目/审核增强 | 抓取速度提升3x，失败率下降80% |
| W1 | 前端样式升级（设计系统/玻璃拟态/排版优化） | UI 视觉达到科技感标准 |
| W2 | 前端交互升级（折叠/搜索/时间戳/标签筛选） | 交互问题全部解决 |
| W2 | AI 分析全栏目覆盖 + 审核日志 | 每个栏目都有AI洞察 |

### Phase 2：架构迁移预研（第2-3周，并行）

**目标**：验证 TrendRadar + Agent 方案可行性

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W2 | TrendRadar 部署测试，导入50源对比抓取效果 | 抓取成功率/速度/质量对比报告 |
| W3 | LangGraph 深度分析 Agent PoC | 单篇新闻深度分析 Demo |
| W3 | 技术选型终审，确定二期方案 | 二期详细开发计划 |

### Phase 3：智能媒体升级（第4-8周）

**目标**：完成架构迁移，上线「单条触发式深度分析」功能，预留付费能力

| 周次 | 任务 | 交付物 |
|------|------|--------|
| W4-W5 | 底层迁移至 TrendRadar，数据层对齐 | 抓取层无缝切换 |
| W5-W6 | 深度分析 Agent 系统开发（5维度输出 + 异步队列 + 缓存） | 单条新闻深度分析 API |
| W6-W7 | 前端交互开发：分析按钮 + 抽屉式结果面板 + 配额显示 | 用户侧完整可用 |
| W7-W8 | 配额系统 + 付费接口预留 + 灰度上线 + 效果优化 | 全功能正式发布，商业化接口就绪 |

---

## 七、资源与风险评估

### 7.1 人力投入

- **一期**：前端 1 人 + 后端 1 人，2 周
- **二期预研**：后端 1 人，1 周（并行）
- **三期**：前端 1 人 + 后端 1.5 人，4 周

### 7.2 成本与商业化评估

**成本侧**：
- **LLM API 成本**：用户按需触发，日均 20-30 次深度分析，月增约 $30-60
- **基础设施**：仍用 GitHub Actions，无额外服务器成本
- **总计**：人力成本为主，API 边际成本极低，单条分析成本约 $0.02-0.05

**收入侧（预留商业化）**：
- **免费额度**：每日 3 次深度分析，覆盖轻度用户
- **付费模式**（二期后可选开启）：
  - 按次付费：0.5 元 / 条深度分析
  - 月度会员：15 元 / 月，每日 20 次额度
- **毛利率预估**：>85%（API 成本占比极低）
- **定位**：增值功能，不影响基础简报免费使用，作为商业化试水

### 7.3 关键风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| TrendRadar 中文源适配不达预期 | 中 | 高 | 一期保底方案可独立运行，不强制迁移 |
| Agent 深度分析质量不达预期 | 中 | 中 | 先上线"辅助分析"定位，不替代人工判断；持续迭代prompt |
| 前端重构工期超预期 | 高 | 中 | 样式和交互分期交付，一期先保视觉升级 |
| LLM 成本暴涨 | 低 | 中 | 分级模型策略 + 缓存 + TOP N 限制 |

---

## 八、终审签字

> 本方案经总经理办公室审议通过，按 Phase 1 启动执行，Phase 2 预研结论出来后再审 Phase 3。

**批准人**：_______________  **日期**：___________

**执行人**：_______________  **日期**：___________

---

*文档结束*
