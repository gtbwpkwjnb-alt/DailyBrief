# AI 深度参与改造方案

## 现状诊断

当前 DailyBrief 的 AI 参与度评分：**3/10**

| 能力维度 | 当前状态 | AI介入度 |
|---------|---------|---------|
| 信息获取 | 50+源RSS/API抓取 | 0% (纯机械) |
| 翻译 | OpenAI翻译英文→中文 | 50% (基本可用但偶有遗漏) |
| 摘要 | 单次LLM生成50-90字摘要 | 50% (信息密度可) |
| 分类标签 | AI分配3-5个标签 | 40% (准确率一般) |
| 栏目总结 | 每栏目80-150字AI总结 | 70% (OK但肤浅) |
| 质量审核 | 简单AI审核pass/fail | 30% (刚上线,粗糙) |
| 深度分析 | ❌ 无 | 0% |
| 跨域关联 | ❌ 无 | 0% |
| 个性化 | ❌ 无 | 0% |
| 交互式分析 | ❌ 无 | 0% |

## 目标：AI 智能媒体平台 (评分 8/10)

```
传统: 抓取 → 翻译 → 展示
                ↓
目标: 抓取 → AI分类 → AI精炼 → AI关联分析 → AI趋势预测 → 个性化推荐 → 交互式探索
```

---

## Phase 1: 立即可以做的 (1-2天)

### 1.1 增强 consolidatedEnrich prompt
当前 consolidated prompt 虽然合并了4个分类调用，但每个条目只做"翻译+标签+重要度"。**增加一个"关联分析"字段**：

```
- 每条输出增加 "related_to": ["条目A的url", "条目B的url"]
  AI自动识别与同分类/跨分类其他条目的关联
  例如: "美联储加息" 关联 "科技股暴跌"、"日元贬值"
```

**改动范围**: 仅改 `enrich.ts` 中 `CONSOLIDATED_SYSTEM_PROMPT_ZH`，增加关联字段输出。

### 1.2 增加 "AI 编辑点评" 字段
在每个条目下方显示一句 **"为什么这很重要"**（Why this matters）：

```
- summary 不变（50-90字事实摘要）
- 新增 "comment" 字段（20-40字编辑点评）
  例如: "这标志着中国AI芯片首次在任天堂生态落地"
```

**改动范围**: `enrich.ts` prompt + `render.ts` article card 增加点评行。

### 1.3 趋势追踪
在 Tag Cloud 下方增加一个 **"热点趋势"** 区域：
- 比较今日标签频率 vs 昨日标签频率
- 显示"🔥 上升最快"标签（今日新增或涨幅最大的）
- 需要缓存昨日标签数据

**改动范围**: 新增 `scripts/trending.ts` 或直接并入 `daily.ts` Phase 6。

---

## Phase 2: 中期改造 (3-5天)

### 2.1 全局AI编辑简报（取代现有digest）
当前 `generateDailyReport` 是孤立调用。改为 **跨分类整合分析**：

```
输入: 所有分类的AI摘要 + 重要度评分 + 关联分析
输出: 
  - 今日头条 (1-3条跨分类最重要新闻)
  - 编辑视角 (AI以"编辑"口吻写一段今日综述)
  - 预警信号 (重要但容易被忽略的条目)
  - 国际视角 (同一事件不同来源的报道角度对比)
```

**改动范围**: `lib/ai/pipeline.ts` 新增 `generateEditorialDigest()` 函数。

### 2.2 多Agent协作分析
引入专家Agent架构（参考 DailyDawn 的设计）：

```
Classifying Agent → 将条目分类到细粒度主题
Question Agent → 对每个主题生成3-5个关键问题
Research Agent → 针对每个问题搜索关联条目
Editor Agent → 整合为结构化简报
```

**改动范围**: 新增 `lib/ai/agents/` 目录，每个 agent 一个文件。

### 2.3 信息源健康监控
- 每次构建记录每个源的成功/失败/响应时间
- 生成源健康仪表板（响应率、失败率趋势）
- 自动禁用连续失败3次的源

**改动范围**: 新增 `lib/monitor/source-health.ts` + 读写 `.cache/health.json`。

---

## Phase 3: 长期演进 (1-2周)

### 3.1 个性化订阅
- 用户可设置兴趣标签（如"关注AI芯片、中美关系"）
- AI根据用户兴趣过滤条目 → 生成个人简报
- 支持多订阅者（小型团队场景）

### 3.2 自定义深度分析
- 对单条新闻，点击"🔍 深度分析" → 调用 `agent-reach` 技能搜索关联背景信息
- 生成 300-500 字深度分析报告
- 包含：时间线、多方观点、数据支撑

### 3.3 交互式探索
- 知识图谱：标签之间的关联网络可视化
- 时间线：某个主题的新闻时间线演进
- 对比视图：同一事件不同来源的报道角度对比

---

## Phase 4: 本机技能集成

### 4.1 接入 luopan 技能
```
场景: 当某个行业出现重大新闻时
触发: 重要度≥8 且 tag含"科技/经济/国际"
动作: 调用 luopan 做该行业的快速产业分析
输出: 产业地图卡片嵌入简报
```

### 4.2 接入 agent-reach 技能
```
场景: 用户点击"深度分析"按钮
动作: 
  1. 提取新闻关键词
  2. 调用 agent-reach 多源检索
  3. LLM整合检索结果为分析报告
  4. 追加到新闻卡片底部
```

---

## 竞品对比: DailyBrief vs 同类工具

| 工具 | AI深度 | 前端 | 部署 | 适合场景 |
|------|--------|------|------|---------|
| **DailyBrief (当前)** | ⭐⭐ | ⭐⭐⭐ | GitHub Actions | 个人AI日报 |
| **DailyBrief (目标)** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | GitHub Actions | 个人/团队AI媒体 |
| news-aggregator | ⭐⭐⭐⭐ | ⭐⭐⭐ | Docker | 全功能AI新闻台 |
| DataCube-AI-Space | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Docker+Vercel | 多语言新闻门户 |
| DailyDawn | ⭐⭐⭐⭐⭐ | ⭐⭐ | GitHub Actions | 深度AI分析实验 |
| PolaNews | ⭐⭐⭐ | ⭐⭐⭐⭐ | Docker | 社交化新闻 |

**结论**: DailyBrief 的 GitHub Actions 部署方式是最轻量的，但 AI 深度是短板。Phase 1-2 的改动可快速提升 AI 评分到 6/10，保持轻量优势。

---

## 优先级建议

```
本周: Phase 1.1 + 1.2 (关联分析 + 编辑点评) 
下周: Phase 2.1 (全局AI编辑简报)
下月: Phase 2.2 + 4.1 (多Agent + 技能集成)
Q3:   Phase 3 (个性化 + 深度分析)
```
